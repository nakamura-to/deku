(function (pot) {
// BEGIN PARSER
{{{parser}}}
// END PRASER

  if (typeof module !== 'undefined') {
    module.exports = parser;
  } else {
    pot.internal.parser = parser;
  }

}(typeof module !== 'undefined' ? {} : pot));